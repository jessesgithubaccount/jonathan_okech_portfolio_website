# Create a project at https://app.tina.io, connect this repo, then copy
# the values it gives you into these three variables (in Netlify's
# site settings > Environment variables, and in a local .env for dev).

TINA_CLIENT_ID=
TINA_TOKEN=
TINA_BRANCH=main
